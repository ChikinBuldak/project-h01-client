import type { Component } from "@/types";

/**
 * A "tag" component.
 * Any entity with this component can be hit by a Hitbox.
 * We will add this to our players.
 */
export class Hurtbox implements Component {}