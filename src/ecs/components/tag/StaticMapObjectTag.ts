import type { Component } from "@/types/ecs";

/**
 * A "tag" component to identify a static piece of map geometry
 * (like a wall or floor).
 */
export class StaticMapObjectTag implements Component {}
